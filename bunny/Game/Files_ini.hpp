#pragma once
#include <SFML/Graphics.hpp>

class Files_ini
{
public:

	Files_ini(sf::RenderWindow& window);
	void objects_ini(sf::RenderWindow& window);
	void draw(sf::RenderWindow& window);

private:

};

